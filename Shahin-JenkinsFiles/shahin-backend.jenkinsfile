pipeline {
    agent {label 'Shahin-Node'}


    stages {
        stage('Build') {
            steps {
                sh 'sudo yarn install'
                            }
            }
    }
}